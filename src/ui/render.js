// §1.7 ui/render.js — Orchestre le rendu, throttlé, visible-only (§1.4)
// TODO: Extraire de index.html :
//   - _renderDirtyPanels(), _panelsDirty, _panelsLastRender (§1.4)
//   - updateDisplays(), renderFooterBB(), renderAchievements(), renderMaterialsPanel()
//   - updateP() (hud principal)

// import { on } from '../core/events.js';

export function initRender() {
    throw new Error('ui/render.js: pas encore extrait de index.html');
}

export function markPanelsDirty() {
    throw new Error('ui/render.js: pas encore extrait de index.html');
}
